{% extends 'base.html.twig' %}

{% block pagetitle %}{{ controller_name }}{% endblock %}

{% block body %}
    <div class="row text-center p-5">
        <a href="{{ path('team_lessons', {id:lesson.team.id}) }}" class="btn btn-danger w-auto m-auto"><i class="bi bi-arrow-return-left"></i> Volver</a>
    </div>
    <h1 class="text-center">{{ lesson.title }}</h1>
    <div id="meet" class="text-center">

    </div>
    <script src='https://meet.jit.si/external_api.js'></script>
    <script>
        const domain = 'meet.jit.si';
        const options = {
            roomName: '{{ lesson.jitsilesson }}',
            width: 700,
            height: 700,
            parentNode: document.querySelector('#meet'),
            lang: 'es'
        };
        const api = new JitsiMeetExternalAPI(domain, options);
    </script>
{% endblock %}
